{% extends 'UerpMainBundle::main.html.twig' %}

{% block body -%}
   <div class="panel panel-info" style="margin: 20px">
  <div class="panel-heading">
       <h1>BankAccount</h1>
  </div>
  <div class="panel-body form-horizontal">



    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Accountname</th>
                <td>{{ entity.accountname }}</td>
            </tr>
            <tr>
                <th>Balance</th>
                <td>{{ entity.balance }}</td>
            </tr>
        </tbody>
    </table>
<div class="btn-group">

        <a class="btn btn-lg btn-default" href="{{ path('bankaccount') }}"><i class="fa fa-arrow-left"></i>
           Back 
        </a>
                <a class="btn btn-lg btn-default" href="{{ path('bankaccount_edit', { 'id': entity.id }) }}"><i class="fa fa-pencil"></i>
            Edit 
        </a>
        {{ form(delete_form) }}

</ul>
</div>
{% endblock %}
