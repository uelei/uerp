{% extends 'UerpMainBundle::main.html.twig' %}

{% block body -%}
    <h1>BankAccount</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Accountname</th>
                <td>{{ entity.accountname }}</td>
            </tr>
            <tr>
                <th>Balance</th>
                <td>{{ entity.balance }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('bankaccount') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('bankaccount_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
