{% extends 'UerpMainBundle::main.html.twig' %}

{% block body -%}
    <h1>BankAccount list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Accountname</th>
                <th>Balance</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('bankaccount_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.accountname }}</td>
                <td>{{ entity.balance }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('bankaccount_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('bankaccount_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('bankaccount_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
