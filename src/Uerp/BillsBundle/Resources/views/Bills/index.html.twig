{% extends 'UerpMainBundle::main.html.twig' %}

{% block body -%}
    <h1>Bills list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Value</th>
                <th>Docorigin</th>
                <th>Dataaux</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('bills_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.value }}</td>
                <td>{{ entity.docorigin }}</td>
                <td>{{ entity.dataaux }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('bills_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('bills_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('bills_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
