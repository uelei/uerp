{% extends 'UerpMainBundle::main.html.twig' %}

{% block body -%}
    <h1>Transactiontype list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Transactiondesc</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('transactiontype_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.transactiondesc }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('transactiontype_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('transactiontype_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('transactiontype_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
